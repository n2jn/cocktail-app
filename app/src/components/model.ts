import {Dimensions} from 'react-native';

export const UNIT_SIZE = Dimensions.get('screen').width / 2;
export const INNER_UNIT_SIZE = Dimensions.get('screen').width / 4;
export const {width: SCREEN_WIDTH, height: SCREEN_HEIGHT} =
  Dimensions.get('screen');
